type localStorageCollectionInterface = {
    active: boolean,
    collection: string,
    duration: string,
    finalDate: string,
    initialDate: string,
    currentDuration: string
}

export default localStorageCollectionInterface;