type localStorageInterface = {
    active: boolean,
    productId: string,
    duration: string,
    finalDate: string,
    initialDate: string,
    currentDuration: string
};

export default localStorageInterface;